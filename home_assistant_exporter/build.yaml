---
# The base image is the upstream exporter image, published from the
# cznewt/home-assistant-exporter repo. This add-on only layers the HA run script.
build_from:
  amd64: ghcr.io/cznewt/home-assistant-exporter:latest
  aarch64: ghcr.io/cznewt/home-assistant-exporter:latest
